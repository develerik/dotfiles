# increase password cache ttl (1 day)
default-cache-ttl 86400
max-cache-ttl 86400
default-cache-ttl-ssh 86400
max-cache-ttl-ssh 86400

# enable ssh support
enable-ssh-support

# tty pinentry
pinentry-program /usr/bin/pinentry-tty
allow-loopback-pinentry
